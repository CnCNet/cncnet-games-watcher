version: "3.9"
services:
    app:
        build: .
        ports:
            - "4000:4000"
        volumes:
            - .:/usr/src/app
            - /usr/src/app/node_modules
        environment:
            NODE_ENV: production
        command: sh -c "npm run build && npm start"
